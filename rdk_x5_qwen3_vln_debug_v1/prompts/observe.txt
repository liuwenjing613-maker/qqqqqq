Mode: OBSERVE_TARGET

Inspect the entire image and decide whether the exact requested target is clearly visible.

Output s="V" only when:
- The requested object itself is clearly visible.
- Its category and visible attributes match the task.
- p can be placed on the geometric center of the actual object body (not an edge, label, or cap).

Otherwise output s="I":
- The target is not confirmed visible.
- Still select one search waypoint using the free-space and semantic-direction rules.
- Prefer a free-floor point leading toward a relevant landmark.
- Do not place p directly on the landmark itself.

Do not confuse a table, shelf, bottle-like object, reflection, label, or similarly colored object with the requested target.
