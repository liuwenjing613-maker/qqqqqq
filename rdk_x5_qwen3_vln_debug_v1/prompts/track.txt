Mode: TRACK_LOCKED_TARGET

The requested target was visible previously.
The previous point on the [0, 1000] grid was:
$previous_point

Inspect the current image again.

If the same exact target is still clearly visible:
- s="V"
- p = geometric center of the visible target body (mid-width / mid-height; not label/cap/edge)
- Do not blindly copy the previous point.

If the target is lost, occluded, or cannot be matched reliably:
- s="I"
- select one visible free-space waypoint most useful for reacquiring the target
- Prefer the last-seen direction only when that direction remains visually open
- Otherwise prefer a safe nearby passage or semantically related region

Do not lock onto a different similar object merely because it is near the old point.
