Mode: VERIFY_CANDIDATE

The last candidate point on the [0, 1000] grid was:
$previous_point

Determine whether the visible candidate matches the full task, including:
- object category
- visible attributes
- explicit spatial relation
- task meaning

If it matches:
- s="S"
- p = center of the verified target body

If it does not match or cannot be confirmed:
- s="F"
- p = next best visible free-space exploration waypoint
- Do not place p on the rejected candidate
- Prefer a different open direction or another semantically relevant region

Be conservative when verifying the target.
