Mode: VERIFY_CANDIDATE
Previous candidate point on [0,1000]: $previous_point
Verify it against the full task: category, attributes, relation, and meaning.

If it matches:
- s="S", a="POINT"
- p = exact center of the verified visible body

Otherwise use s="F":
- Never point at the rejected candidate.
- Use POINT on a connected visible free-floor route.
- If the front ends at a wall/cabinet but floor continues around one side, place POINT clearly inside that side passage at mid-distance to begin a gradual turn; never use central floor ending at the blocker.
- Prefer the wider, deeper, safer side and keep it unless evidence changes.
- Use TURN only when the route is outside view and no side-floor point exists.

Be strict. If direction evidence is weak, choose the safest deepest connected floor POINT, never a default center point.
